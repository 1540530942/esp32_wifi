# 持久项目上下文

开始任何实现、排障或发布工作前，依次阅读：

1. `WORKING.md`：协作、提交和发布规则。
2. `docs/PROJECT_CONTEXT.md`：当前源码边界、已知部署注意事项和文档入口。
3. 与任务相关的 `docs/` 文档；需要历史证据时查看 `docs/history/`。

历史文档只用于追溯背景。当前行为、接口和发布操作必须以仓库现有源码及发布文档为准，并在涉及线上服务时重新核验运行态。
