# 历史记录

这里保存不参与构建的联调、部署和排障证据，供后续排查时追溯背景；它们不是当前接口或发布流程的权威规范。

- `ESP32_WANGYUTANG_PLAN_2026-08_09.md`：从原临时工作区归档的完整历史计划和联调记录，2026-09-13 迁入。其中包括 OTA、音频、LCD、云端联调，以及 DGX Spark 外接小屏排障过程。

当前实现以仓库中的 `esp32_wifi_impl/` 和 `cloud/device_hub/` 为准；部署和 OTA 操作请先阅读 [`../OTA_RELEASE.md`](../OTA_RELEASE.md)。
