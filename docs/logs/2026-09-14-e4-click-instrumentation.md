# 2026-09-14 · E4 打断延迟：把缺的那一半时间戳补上

## 背景：状态文档里的一句话是错的

`2026-09-14-aec-bargein-status.md` 之前写：

> E4 还缺的东西：素材开头的 20ms 咔哒声还没做。**ESP32 侧的时间戳埋点已就位**——
> `afe_pipeline.c` 在打断触发时打印 `local barge-in -> duck+stop at t=<微秒>`。

这句话把"一半"说成了"已就位"。E4 要测的是两个时间戳之差：

```
延迟 = t_duck（停播） − t_click（人开口）
```

**`t_duck` 有埋点，`t_click` 完全没有实现。** 只有停播时刻、没有开口时刻，这个实验
根本跑不起来——不是"等树莓派回来就能跑"。这次把缺的一半补上了，而且这部分工作
**完全不需要树莓派**。

## t_click 为什么必须在 AFE 之前测

放在 AFE 输出上测会把 AEC/NS/VAD 的处理延迟从测量值里**减掉**——而那正是这个实验
要量的东西。所以检测放在 `feed_task` 里，直接扫 `board_mic_read()` 拿到的**原始麦克风
通道**（ch0），在喂给 AFE 之前。

## 阈值为什么必须是相对的

E4 进行时 ESP32 自己正在播放，原始麦克风里本来就有机器人的回声。固定绝对阈值两头
不讨好：定低了在高音量下会被回声触发，定高了在低音量下漏掉咔哒。

所以用**相对于慢速包络的倍数**：

```c
#define CLICK_ENV_ALPHA      0.05f   // ~20 帧（~320ms）时间常数
#define CLICK_TRIGGER_RATIO  4.0f    // 高出running包络 +12dB
#define CLICK_MIN_PEAK       2000    // 绝对下限，防止安静时自激
#define CLICK_SEED_FRAMES    16      // 包络收敛前不判定
```

包络跟着回声电平走，咔哒要高出它 12dB 才算数。`CLICK_SEED_FRAMES` 是防止刚 arm
时包络还是 0、任何声音都满足"4 倍"的问题。

## 帧内定位：不要被 16ms 量化

`s_feed_chunksize` 在 16kHz 下是 256 样本 ≈ 16ms。如果只记"哪一帧检测到"，测量值
就会有 16ms 的量化误差——占 200ms 预算的 8%，白白吃掉一格精度。

所以扫描帧内第一个越过阈值的样本下标，再换算回时间：

```c
s_click_us = frame_end_us - (int64_t)(chunk - first) * 1000000 / 16000;
```

`frame_end_us` 是整帧读完的时刻，所以第 `first` 个样本发生在它之前
`(chunk - first)` 个样本的时间。

## 一次性检测，不是持续检测

第一个合格的上升沿记下时间戳就 **disarm**。否则后面的语音会不断覆盖 `t_click`，
最后量到的是"最后一次大声"到停播的间隔，而不是"开口"到停播。

## 结果必须能远程读到

这次特意加了 `click_result` 命令，而不是只打 UART 日志：

```
click_arm     -> done|armed
click_result  -> done|latency=<N> ms click=<us> duck=<us>
```

理由是刚在 T3.3 上吃过一次亏（见 `t33-position` 末尾）：`dropped=` 只打在串口，
设备上报云端的日志缓冲里只有命令回执，结果那个数**远程根本验证不了**。E4 的延迟是
这次工作的最终验收指标，绝不能是一个只有插 USB 才能看到的数。

## 咔哒素材

`tools/aec_bench/make_click_fixtures.py`，已在 spark 上生成：

```
turn01_user_click.wav: 4.32s -> 4.34s
turn03_user_click.wav: 93.12s -> 93.14s
turn05_user_click.wav: 4.08s -> 4.10s
turn07_user_click.wav: 3.92s -> 3.94s
turn09_user_click.wav: 3.36s -> 3.38s
```

位置：`spark:~/workspace/data/aec/dialogue_wenyanwen/`（24kHz 单声道，树莓派 aplay 直读）。

实测 turn05 开头：**咔哒峰值 24588（约 -2.5 dBFS），紧随其后是静音**——咔哒确实是
开头最响的东西，检测器不会被原素材的起始音盖过。

两个设计细节：

1. **用宽带噪声突发而不是纯音**。纯音可能正好落在房间响应的零点里；而且咔哒要在
   混有回声的原始麦克风信号里被认出来。
2. **20ms 内衰减到 0**（`(1 - i/n)²`）。平顶突发在末尾会产生第二个瞬变，检测器同样
   会对它感兴趣，记录的起点就会有一个咔哒宽度的歧义。

## 咔哒本身不会触发打断（这是有意的）

打断需要 `CONFIG_AEC_BARGEIN_SPEECH_FRAMES`(4) 帧持续语音 ≈ 64ms，而咔哒只有 20ms。
所以**停播仍然是被咔哒之后的语音触发的**，量到的是真正的"开口→静音"，不是一个
咔哒回路的往返时间。

## 状态

- 固件：`click_arm` / `click_result` 两条命令 + `feed_task` 里的检测器，本地编译通过。
- 素材：5 个 `_click` 版本已在 spark 上生成并抽查过电平。
- **仍然阻塞在树莓派**：E4 需要它扮演开口的人。ESP32 侧现在真的准备好了。

## E4 恢复后的执行步骤

```bash
# 1. 把 click 素材推到树莓派（它掉线期间没同步过）
scp ~/workspace/data/aec/dialogue_wenyanwen/turn05_user_click.wav pi:<素材目录>/

# 2. ESP32 开始播一段长素材
curl -X POST 'https://www.wangyutang.cn/devices/api/device/esp32-s3-walle/play_lan_audio' \
  -H 'Content-Type: application/json' \
  -d '{"params":{"name":"turn04_assistant.wav"},"settings_override":{"voice_volume_percent":80}}'

# 3. 等过了 onset grace（500ms）再 arm，然后让树莓派播 click 素材
curl -X POST '.../command' -d '{"action":"click_arm"}'
#    树莓派 play_local_audio turn05_user_click.wav

# 4. 读结果
curl -X POST '.../command' -d '{"action":"click_result"}'
# 期望 latency < 200ms（好 < 120ms）
```

**注意 arm 的时机**：要在 ESP32 已经开始播、且过了 500ms onset grace 之后再 arm，
否则包络是在"没有回声"的状态下收敛的，等回声起来时会误判。

---

## 补记：v48 上机自检,检测器被自己的回声打中了

按计划做了一次**不需要树莓派的负向验证**:ESP32 自己播 turn04(vol80),播了 3s 后
`click_arm`,让检测器在纯回声环境里跑 12s。期望是"没检测到"。

实测:

```
c-8f82a3 action=click_arm    status=done|armed              ts=42.37
c-10936e action=click_result status=done|click=42648695 us  ts=58.34
```

**arm 后约 280ms 就触发了**,被机器人自己的语音打中。

### 根因不是阈值,是判据本身不成立

我原本的判据是"ch0 峰值高出 320ms 滑动包络 12dB"。问题在于**语音是高度非平稳的**
——一个音节起音清清楚楚就能高出滑动包络 12dB,和咔哒在这个指标上无法区分。把倍数
调高只会变成"漏掉咔哒"和"被回声触发"之间挪位置,不解决问题。

### 真正的判据:ch1 硬件回采

能区分两者的信息硬件早就提供了:

| | ch0 麦克风 | ch1 回采 |
|---|---|---|
| 机器人自己的语音起音 | 有 | **有**(P1 实测还高 9-10dB) |
| 树莓派的咔哒 | 有 | **没有** |

ch1 是功放输出的电气抽头,不拾环境声——P1 那次量电平时的旁证:不播放时 ch1 掉到
-84.3 dBFS(几乎绝对静默)而 ch0 还有 -52.1 dBFS 的房间噪声。

所以判据改成**双通道**:ch0 跳起来(+12dB)**且** ch1 没跟着跳(+6dB 以内)才算数。
代价是每帧多算一个峰值。

```c
const bool ref_quiet =
    nch < 2 || (float)peak_ref <= s_click_env_ref * CLICK_REF_QUIET_RATIO + (float)CLICK_MIN_PEAK;
```

`nch < 2` 那一路（没有回采通道时）保留了单通道判据,但那正是会误触发的配置,
注释里写明了,免得以后有人在别的板子上复用时以为它靠谱。

### 这次验证的方法论价值

**如果不做这个负向自检,E4 一跑就会得到一个"看起来很漂亮"的延迟数字**——检测器
被回声触发的时刻通常早于真人开口,算出来的 latency 会偏大甚至为负,而两个时间戳
都"有值",从结果上完全看不出是假的。

这和 E5 那次("误触发计数三轮都是 0,实际藏着两层回归")、T3.3 那次(寄存器保持原值
读数证明不了任何事)是同一类问题的第三次出现:**必须构造一个"应该失败"的场景,
看它是不是真的失败。**只验证"该成功时成功"的话,一个永远返回成功的桩也能通过。

树莓派还没回来,所以正向验证(真咔哒能被检出)仍然做不了。现在的状态是:
**负向已验证(回声不再触发),正向待验证。**

---

## 再补记：v49 的修复无效，门控写错了

v49 上机复跑同一个负向自检（播 turn04 vol80，3s 后 arm，跑 12s），**仍然触发**：

```
c-fbc4ef action=click_arm    status=done|armed
c-b3e8c8 action=click_result status=done|click=27590904 us, no barge-in yet
```

### 错在哪

我写的是：

```c
ref_quiet = peak_ref <= env_ref * 2.0 + FLOOR      // "ch1 没跳得太多就算安静"
```

播放时 ch1 一直很响，`env_ref` 也跟着高，**一个中等幅度的 ch1 上升照样满足这个不等式**
——门控几乎恒为真，等于没加。我把"ch1 有没有能量"和"ch1 有没有跳"混成了一个条件，
而实际需要的是第三个量。

### 正确的量：两通道的比值

回声路径增益 G 大致固定，所以只有机器人在响时 `peak0 ≈ G · peak_ref`：

- **机器人音节起音**：两个通道一起涨，**比值不变**
- **外部咔哒**：只涨分子，**比值跳起来**

所以判定改成对 `ratio = peak0 / peak_ref` 的包络做倍数检测。这个形式还有个好处：
不播放时 `peak_ref ≈ 0`，分母被 `+1` 托底，`ratio` 退化成 `peak0` 本身，判据自动变回
朴素的 ch0 电平检测——不需要为"播放中/没播放"写两套逻辑。

### 一个必须处理的时序细节

ch1 是电气抽头，比声学路径**领先一个"喇叭→麦克风"的飞行时间**。所以一个正好压在
帧边界上的机器人起音，可能 ch1 落在第 N 帧、ch0 落在第 N+1 帧——第 N+1 帧看起来就是
"只有 ch0 涨"，和咔哒一模一样。

取本帧和上一帧 ch1 峰值的较大者作分母，覆盖这个偏斜：

```c
const int16_t ref_win = peak_ref > s_click_prev_ref ? peak_ref : s_click_prev_ref;
```

**不做这一步的话，每一个跨帧边界的音节都会被判成咔哒。**

### 记一笔：这是同一个 bug 的第二次

v48 和 v49 都是"判据选错了"，不是参数没调好。两次的共同点是**我用了一个看起来相关、
但实际不构成区分的量**（v48：ch0 相对自己的包络；v49：ch1 相对自己的包络）。真正
有区分力的是**两个通道之间的关系**，而不是任一通道相对自己的历史。

也说明一件事：这个负向自检每次都如实地把问题顶了出来，两次修复都是它挡下来的。

---

## 三补记：v50 通过，但"通过"需要一个正向对照才算数

v50 上机复跑，两个负向场景都通过：

```
播放中（回声）  → no click detected since arming
完全静音        → no click detected since arming
```

v48 触发 → v49 触发 → v50 不触发，**同一个测试从失败翻到通过**，这才是修复有效的
证据（只看修复后通过一次，分不清是真修好了还是测试没跑到）。

### 但这还不够

**一个永远不触发的检测器，上面两个测试同样全过。** 负向测试单独存在时，"正确地不
触发"和"根本不会触发"在结果上完全一样。

树莓派不在，ESP32 唯一的发声途径是自己的喇叭（必然同时进 ch1），**没有天然的外部
声源可用**。所以用了这个仓库里已经用过一次的办法（v44 那次证明 PGA 配置真的写进了
寄存器）：把阈值做成 Kconfig，临时调到会被回声触发的水平。

`CONFIG_AEC_CLICK_TRIGGER_RATIO_X10`，默认 40（4.0x / +12dB），v51 临时设 11。

```
v51 (1.1x)  播放中 arm → click=26569425 us      ← 触发
v50 (4.0x)  播放中 arm → no click detected      ← 不触发
```

同一段回声、同一个场景，**只有阈值不同、行为跟着变**。证明了：

1. `click_scan` 确实在运行（不是没被调用）
2. 帧内时间戳换算产出的是设备时钟上的合理值（不是 0、不是垃圾）
3. `click_result` 能把它报回来

v52 已改回 40。

### 仍未验证的一行

`latency_ms = (duck_us - click_us) / 1000` 这段配对算术**从未执行过**——两次触发都
是 `no barge-in yet`，因为机器人的回声本来就不该触发打断（E5 已验收 0 次误打断）。

这是 E4 最终验收数字的来源，目前只有代码依据。要跑到它，必须有真人声（或树莓派）
既触发咔哒、又触发打断——也就是 E4 本身。**这一行只能等树莓派。**

## 当前状态汇总（E4）

| 环节 | 状态 |
|---|---|
| t_click 检测器 | ✅ 实现，负向+正向双向验证 |
| t_duck 时间戳 | ✅ 原有 |
| 两者配对算延迟 | ⚠️ 代码就绪，**从未执行** |
| 咔哒素材 | ✅ 5 个 `_click` 版本已生成 |
| 树莓派播放 | ❌ 阻塞 |
