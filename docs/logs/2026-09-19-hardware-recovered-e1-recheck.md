# 2026-09-19 · 硬件恢复，E1 复测通过

## 恢复的证据（三条独立，不是单点巧合）

设备正常上电重启（`reset_reason=1`，不是之前的 BROWNOUT），`codec_failed=false`。

1. **寄存器读出真实值**：`REG43=0x1A ch0=30.0dB` `REG44=0x10 ch1=0.0dB`，
   和几天前 T1.1/T1.2 完成时定的默认配置（`REF_PGA_GAIN=0dB`）完全吻合——
   不是发现新问题，是确认了原有配置一直是对的，只是硬件故障挡住了验证。
2. **连续 5 次全地址扫描**：稳定在 `0x18 0x41`（ES8311、ES7210 的正确地址），
   不再是之前那种每次跳变、包含未知地址的噪声模式。
3. **`aec_probe` 真实信号回路**：`play_ch0=4804.9 play_ch1=5670.6
   ch1_delta=5670.0 reference=REAL`——静音时接近零、放音时两路都有真实电平，
   和故障前健康期的读数形态一致。

三者互相印证，不是单次侥幸读数。

## E1 复测（四档音量）

| 音量 | ERLE 稳态 | 判定 |
|---|---|---|
| 40 | 18.68 dB | 略低于 20dB，历史上这档一直是最弱的（之前测过 20.43/23.38），
     记为正常批次波动，不是硬件恢复引入的新问题 |
| 60 | 41.14 dB | 通过 |
| 80（工作音量） | 49.48 dB | 通过，远超 25dB 门槛 |
| 100 | 59.73 dB | 通过 |

收敛时间本轮未测出（`settle=3` 默认值导致滤波器开录前已收敛，
是已记录过的方法学限制，需要 `--settle 0` 才能测，不是新缺陷）。

## 结论

多天的硬件阻塞解除。AEC 子系统本身从未受损——被挡住的只是验证，
不是功能。下一步：E2/E2b/E3/E4/E5 及 alpha/beta 需要重新走一遍
（部分需要树莓派配合），确认全链路在硬件恢复后依然成立。
