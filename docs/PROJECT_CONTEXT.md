# 项目上下文

最后整理：2026-09-13。

## 权威源码

- ESP32 固件：`esp32_wifi_impl/`
- 云端设备控制面：`cloud/device_hub/`
- OTA 发布与现场操作：[`OTA_RELEASE.md`](OTA_RELEASE.md)
- 完整历史联调与排障证据：[`history/`](history/)

## 延续工作前的注意事项

- 先阅读仓库根目录的 `WORKING.md`，再修改或发布。
- 当前分支用于 AEC 全双工工作；不要把无关的本地修改混入提交。
- 云端的运行容器可能存在未回写仓库的热修复。若需重建或发布服务，先比较部署机源码、容器内运行文件和本仓库文件，避免覆盖线上修复。
- 2026-09-13 的核对中，云端 `server.py` 与静态 `device.html` 和运行容器一致；运行中的 `ota_manager.py` 比部署目录源码多一项 OTA 完成状态的超时限制。该信息是排查线索，发布前必须重新核验。
